# Loaded automatically by Vite when running in development mode (npm run dev).
# Backend API base URL — matches the local ASP.NET Core dev port (see
# backend/RhythaalayaLog.API/Properties/launchSettings.json → applicationUrl).
VITE_API_URL=http://localhost:5101/api

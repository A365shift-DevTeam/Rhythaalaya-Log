# Loaded automatically by Vite in development mode (`npm run dev`).
# Local ASP.NET Core dev server — see backend/RhythaalayaLog.API/appsettings.Development.json
# (Cors:AllowedOrigins already lists http://localhost:3000).
VITE_API_URL=https://localhost:7101/api

# Loaded automatically by Vite in production mode (`npm run build`, `npm run preview`).
# Must point at the deployed backend, and that origin must be listed in the backend's
# Cors:AllowedOrigins (backend/RhythaalayaLog.API/appsettings.json).
VITE_API_URL=https://batchlyapi.ambot365.com/api
